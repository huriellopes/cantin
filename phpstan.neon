includes:
    - vendor/larastan/larastan/extension.neon
    - phpstan-baseline.neon

parameters:
    paths:
        - app

    level: 5

    # Reduz ruído de código gerado/legado; ajuste o nível conforme for limpando.
    reportUnmatchedIgnoredErrors: false
